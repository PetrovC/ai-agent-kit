# Local / OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/settings.json
.idea/
*.swp
*.swo

# Secrets
.env
.env.*
*.local.json
*.local.toml

# Node (if scripts use it later)
node_modules/

# Python (if scripts use it later)
__pycache__/
*.pyc

# AI - Claude Code session/runtime and install output.
# The kit's Claude SOURCE lives in tooling/claude/, never in root .claude/.
/.claude/

# AI - kit install OUTPUTS must never be committed into the kit itself.
# docs/ai/ is intentionally tracked here as project-owned context for
# ai-agent-kit itself. Target-project templates still live in project-template/.
/AGENTS.md
/CLAUDE.md
/GEMINI.md
/.geminiignore
/.mcp.json
/.mcp.example.jsonc
/.kit-version
/.kit-manifest
/.codex/
/.gemini/
/.agents/
CLAUDE.local.md
