## Issue #159 — Approximate token logger (PostToolUse hook)

**Task type:** feat
**Branch to create:** agent/agy/claude-sonnet-4-6/feat/token-logger-159

---

### Start here — ALWAYS do this first before any file changes

```bash
git checkout master
git pull origin master
git checkout -b agent/agy/claude-sonnet-4-6/feat/token-logger-159
```

---

### Background

Users want to track approximate token usage per session so they can monitor
cost across versions. Claude Code's `PostToolUse` hook receives a JSON payload
on stdin with `tool_name`, `tool_input`, and `tool_response`. We can estimate
tokens by counting characters / 4.

The hook is **opt-in** — it must NOT be wired in `tooling/claude/settings.json`
by default. It ships as a documented file that users add manually.

---

### File 1 — Create `tooling/claude/hooks/token-log.sh`

```bash
#!/usr/bin/env bash
if [[ -n "${AAK_DEBUG:-}" && "${AAK_DEBUG}" != "0" && "${AAK_DEBUG}" != "false" ]]; then set -x; fi
# token-log.sh — PostToolUse hook
#
# Appends an approximate token-usage entry to .claude/session-log/token-log.jsonl
# each time a tool call completes. Token counts are estimated (chars / 4) —
# not billing-accurate. Useful for tracking session cost trends over time.
#
# OFF BY DEFAULT. To enable, add to .claude/settings.json under hooks.PostToolUse:
#   {
#     "matcher": "",
#     "hooks": [{
#       "type": "command",
#       "command": "bash \"${CLAUDE_PROJECT_DIR}/.claude/hooks/token-log.sh\"",
#       "async": true
#     }]
#   }
set -euo pipefail

ROOT="${CLAUDE_PROJECT_DIR:-$PWD}"
[[ -d "$ROOT" ]] || exit 0
cd "$ROOT" || exit 0

LOG_DIR=".claude/session-log"
mkdir -p "$LOG_DIR"
LOG_FILE="$LOG_DIR/token-log.jsonl"

# Read JSON payload from stdin; exit gracefully if no input.
PAYLOAD=""
if read -t 1 -r line 2>/dev/null; then
    PAYLOAD="$line"
    while IFS= read -t 0.1 -r extra 2>/dev/null; do
        PAYLOAD="$PAYLOAD$extra"
    done
fi

[[ -z "$PAYLOAD" ]] && exit 0

TOOL_NAME="unknown"
INPUT_CHARS=0
OUTPUT_CHARS=0

if command -v python3 >/dev/null 2>&1; then
    read -r TOOL_NAME INPUT_CHARS OUTPUT_CHARS <<< "$(python3 - <<'PYEOF'
import sys, json
try:
    d = json.loads(sys.argv[1]) if len(sys.argv) > 1 else json.load(sys.stdin)
    name = d.get("tool_name", "unknown")
    inp   = len(json.dumps(d.get("tool_input", {})))
    out   = len(json.dumps(d.get("tool_response", {})))
    print(name, inp, out)
except Exception:
    print("unknown", 0, 0)
PYEOF
)" <(echo "$PAYLOAD") 2>/dev/null || true
fi

# Fallback: estimate from raw payload length
if [[ "$INPUT_CHARS" -eq 0 && "$OUTPUT_CHARS" -eq 0 ]]; then
    INPUT_CHARS=${#PAYLOAD}
fi

# Approximate tokens at 4 chars / token
INPUT_TOKENS=$(( INPUT_CHARS / 4 ))
OUTPUT_TOKENS=$(( OUTPUT_CHARS / 4 ))
TOTAL_TOKENS=$(( INPUT_TOKENS + OUTPUT_TOKENS ))

TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

printf '{"ts":"%s","tool":"%s","input_chars":%d,"output_chars":%d,"approx_input_tokens":%d,"approx_output_tokens":%d,"approx_total_tokens":%d}\n' \
    "$TIMESTAMP" "$TOOL_NAME" "$INPUT_CHARS" "$OUTPUT_CHARS" \
    "$INPUT_TOKENS" "$OUTPUT_TOKENS" "$TOTAL_TOKENS" \
    >> "$LOG_FILE"

exit 0
```

Make it executable: `chmod +x tooling/claude/hooks/token-log.sh`

---

### File 2 — Update `tooling/claude/CLAUDE.md`

Find the section that lists hooks (search for "session-summary" or "hooks"). Add a
new bullet in the same style for the token logger. If there is no dedicated hooks
listing, find the "Session hygiene" section and append after it:

```markdown
## Token logger (opt-in)

`tooling/claude/hooks/token-log.sh` appends per-tool-call approximate token
estimates to `.claude/session-log/token-log.jsonl`. To enable, add it to
`PostToolUse` in `.claude/settings.json`:

```json
{
  "matcher": "",
  "hooks": [{
    "type": "command",
    "command": "bash \"${CLAUDE_PROJECT_DIR}/.claude/hooks/token-log.sh\"",
    "async": true
  }]
}
```

Token counts are estimated (chars / 4). Use them for session cost trends, not
billing reconciliation.
```

---

### File 3 — Update `CHANGELOG.md`

Under `## [Unreleased]` → `### Added`, prepend:

```
- **`feat(hooks)` — add opt-in approximate token logger hook (#159).**
  `tooling/claude/hooks/token-log.sh` is a new `PostToolUse` hook that appends
  per-call approximate token usage to `.claude/session-log/token-log.jsonl`.
  Token counts are estimated (input/output chars / 4). Disabled by default;
  opt-in via `settings.json`.
```

---

### Constraints

- Only touch `tooling/claude/hooks/token-log.sh`, `tooling/claude/CLAUDE.md`, `CHANGELOG.md`
- Do NOT modify `tooling/claude/settings.json` — hook must stay opt-in
- Hook must run without error when stdin is empty (exit 0 gracefully)
- Use `set -euo pipefail` + `AAK_DEBUG` trace guard (same as other hooks)
- Script must pass shellcheck: `shellcheck --severity=warning -e SC1090,SC1091 tooling/claude/hooks/token-log.sh`

---

### After making changes

1. `shellcheck --severity=warning -e SC1090,SC1091 tooling/claude/hooks/token-log.sh`
2. `git add tooling/claude/hooks/token-log.sh tooling/claude/CLAUDE.md CHANGELOG.md`
3. `git commit -m "feat(hooks): add opt-in approximate token logger hook (#159)"`
4. `git push origin agent/agy/claude-sonnet-4-6/feat/token-logger-159`
5. `gh pr create --title "feat(hooks): add opt-in approximate token logger hook (#159)" --body "Closes #159

Adds tooling/claude/hooks/token-log.sh — a PostToolUse hook that appends approximate token usage to .claude/session-log/token-log.jsonl. Disabled by default; opt-in via settings.json PostToolUse block. Token counts estimated at chars/4." --base master`

---

### Acceptance criteria

- [ ] `token-log.sh` exists and is executable
- [ ] Graceful exit when stdin is empty
- [ ] shellcheck passes
- [ ] CLAUDE.md documents opt-in setup
- [ ] CHANGELOG entry added
