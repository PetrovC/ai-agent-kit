## Issue #156 — Minimal statusline: context% + cache age

**Task type:** feat
**Branch to create:** agent/agy/claude-sonnet-4-6/feat/statusline-156

---

### Start here — ALWAYS do this first before any file changes

```bash
git checkout master
git pull origin master
git checkout -b agent/agy/claude-sonnet-4-6/feat/statusline-156
```

---

### Background

Users have no visibility into session cost while working. We already have
`tooling/claude/hooks/token-log.sh` (PostToolUse, opt-in) that writes per-call
token estimates to `.claude/session-log/token-log.jsonl`. The statusline reads
that file and emits a one-line status after each tool call.

**What to show:**
```
[aak] ctx: 4% (~8 400 tok) | 14 calls | cache: fresh (< 5 min)
```

- `ctx %` = sum of `approx_total_tokens` in today's token-log.jsonl ÷ 200 000
  (200k is Claude Sonnet 4's context window; hard-code as the default, make it
  overridable via env var `AAK_CONTEXT_WINDOW`, default 200000).
- `calls` = number of lines (entries) in today's token-log.jsonl.
- `cache` = age of the most recent `.claude/session-log/pre-compact-*.md` file,
  if any exists. If no compact snapshot exists: "fresh (new session)".
  If last compact < 5 min ago: "fresh (< 5 min)".
  If < 1 hour: "< Xm".
  If >= 1 hour: "Xh Ym".

**Performance requirement:** exit in ≤ 50 ms. Do NOT use Python for the main
path — use pure bash + awk for the JSONL sum.

---

### STEP 1 — Create `tooling/claude/hooks/statusline.sh`

Write the script at `tooling/claude/hooks/statusline.sh`.

```bash
#!/usr/bin/env bash
if [[ -n "${AAK_DEBUG:-}" && "${AAK_DEBUG}" != "0" && "${AAK_DEBUG}" != "false" ]]; then set -x; fi
# statusline.sh — PostToolUse hook (async, opt-in)
#
# Prints a one-line context-usage summary after each tool call, so you can
# see session cost signals before drift.
#
# Output format (stdout):
#   [aak] ctx: 4% (~8 400 tok) | 14 calls | cache: < 5 min
#
# Context % is estimated: cumulative session tokens ÷ AAK_CONTEXT_WINDOW (default 200 000).
# Token counts come from .claude/session-log/token-log.jsonl written by token-log.sh.
# If token-log.sh is not enabled, the statusline skips silently.
#
# OFF BY DEFAULT. To enable, add BOTH token-log.sh AND this script to
# PostToolUse in .claude/settings.json. See CLAUDE.md "## Statusline (opt-in)".
set -euo pipefail

ROOT="${CLAUDE_PROJECT_DIR:-$PWD}"
[[ -d "$ROOT" ]] || exit 0
cd "$ROOT" || exit 0

LOG_DIR=".claude/session-log"
LOG_FILE="$LOG_DIR/token-log.jsonl"

# If token-log.jsonl doesn't exist, nothing to show.
[[ -f "$LOG_FILE" ]] || exit 0

CTX_WINDOW="${AAK_CONTEXT_WINDOW:-200000}"
TODAY=$(date +"%Y-%m-%d")

# Sum approx_total_tokens for entries from today using awk (fast, no python needed)
read -r TOTAL_TOKENS CALL_COUNT <<< "$(awk -v today="$TODAY" '
  BEGIN { total=0; calls=0 }
  index($0, today) > 0 && /"approx_total_tokens"/ {
    match($0, /"approx_total_tokens":[[:space:]]*([0-9]+)/, arr)
    if (arr[1] != "") { total += arr[1]; calls++ }
  }
  END { print total, calls }
' "$LOG_FILE" 2>/dev/null || echo "0 0")"

TOTAL_TOKENS="${TOTAL_TOKENS:-0}"
CALL_COUNT="${CALL_COUNT:-0}"

# Context % (integer)
if [[ "$CTX_WINDOW" -gt 0 ]]; then
  CTX_PCT=$(( TOTAL_TOKENS * 100 / CTX_WINDOW ))
else
  CTX_PCT=0
fi

# Format token count with thousands separator (awk)
TOKENS_FMT=$(awk "BEGIN { printf \"%'d\", $TOTAL_TOKENS }" 2>/dev/null || echo "$TOTAL_TOKENS")

# Cache age: find most recent pre-compact snapshot
CACHE_LABEL="fresh (new session)"
COMPACT_FILE=$(ls -t "$LOG_DIR"/pre-compact-*.md 2>/dev/null | head -1 || true)
if [[ -n "$COMPACT_FILE" ]]; then
  NOW_EPOCH=$(date +%s)
  # Use stat for file mtime (portable: try GNU stat, fall back to date)
  FILE_EPOCH=$(stat -c %Y "$COMPACT_FILE" 2>/dev/null \
    || stat -f %m "$COMPACT_FILE" 2>/dev/null \
    || echo "$NOW_EPOCH")
  AGE_SEC=$(( NOW_EPOCH - FILE_EPOCH ))
  if [[ "$AGE_SEC" -lt 300 ]]; then
    CACHE_LABEL="fresh (< 5 min)"
  elif [[ "$AGE_SEC" -lt 3600 ]]; then
    AGE_MIN=$(( AGE_SEC / 60 ))
    CACHE_LABEL="< ${AGE_MIN}m"
  else
    AGE_HR=$(( AGE_SEC / 3600 ))
    AGE_MIN=$(( (AGE_SEC % 3600) / 60 ))
    CACHE_LABEL="${AGE_HR}h ${AGE_MIN}m"
  fi
fi

printf '[aak] ctx: %d%% (~%s tok) | %d calls | cache: %s\n' \
  "$CTX_PCT" "$TOKENS_FMT" "$CALL_COUNT" "$CACHE_LABEL"

exit 0
```

**Important**: set the executable bit after creating the file:
```bash
chmod +x tooling/claude/hooks/statusline.sh
git update-index --chmod=+x tooling/claude/hooks/statusline.sh
```

---

### STEP 2 — Add `## Statusline (opt-in)` section to `tooling/claude/CLAUDE.md`

Find the `## Token logger (opt-in)` section. Add a NEW section **immediately after** it:

```markdown
## Statusline (opt-in)

`tooling/claude/hooks/statusline.sh` prints a one-line context-cost summary
after each tool call:

```
[aak] ctx: 4% (~8 400 tok) | 14 calls | cache: < 5 min
```

Requires `token-log.sh` to be enabled first (it provides the data). Add both
hooks to `PostToolUse` in `.claude/settings.json`:

```json
{
  "matcher": "",
  "hooks": [
    {
      "type": "command",
      "command": "bash \"${CLAUDE_PROJECT_DIR}/.claude/hooks/token-log.sh\"",
      "async": true
    },
    {
      "type": "command",
      "command": "bash \"${CLAUDE_PROJECT_DIR}/.claude/hooks/statusline.sh\"",
      "async": true
    }
  ]
}
```

Context % is estimated: cumulative session tokens ÷ `AAK_CONTEXT_WINDOW`
(default 200 000 for Claude Sonnet 4). Override: `export AAK_CONTEXT_WINDOW=100000`.
```

**IMPORTANT CI rule**: The `pr-router-parity.yml` workflow checks that every
`##` heading in `tooling/claude/CLAUDE.md` also exists in `tooling/codex/AGENTS.md`
and `tooling/agy/AGY.md`, OR is in the EXCEPTIONS array.

"Statusline" is Claude-specific (other tools don't have hooks). You MUST add it
to the EXCEPTIONS array in `.github/workflows/pr-router-parity.yml`:

Find the `EXCEPTIONS=(` array and add `"Statusline"` (just the word, not the
full heading — the check uses prefix matching):

```bash
EXCEPTIONS=(
  ...
  "Token logger"
  "Worktree settings"
  "Statusline"     # ← add this line
)
```

---

### STEP 3 — Add `shellcheck` pass

Run:
```bash
shellcheck --severity=warning -e SC1090,SC1091 tooling/claude/hooks/statusline.sh
```

Fix any warnings before committing.

---

### STEP 4 — Update `CHANGELOG.md`

Under `## [Unreleased]` → `### Added`, prepend:

```
- **`feat(claude)` — opt-in statusline showing context% and cache age (#156).**
  `tooling/claude/hooks/statusline.sh` is a PostToolUse hook that prints
  `[aak] ctx: N% (~X tok) | N calls | cache: <age>` after each tool call.
  Requires `token-log.sh` to be enabled. Context % estimated from session
  cumulative tokens ÷ `AAK_CONTEXT_WINDOW` (default 200 000).
```

---

### Constraints

- Only touch: `tooling/claude/hooks/statusline.sh`, `tooling/claude/CLAUDE.md`,
  `.github/workflows/pr-router-parity.yml`, `CHANGELOG.md`
- Do NOT modify `tooling/claude/settings.json` (the hook is opt-in and users
  wire it manually, just like token-log.sh)
- Output must be a single line
- Pure bash + awk only (no python in the hot path)
- shellcheck must pass

---

### After making changes

1. `shellcheck --severity=warning -e SC1090,SC1091 tooling/claude/hooks/statusline.sh`
2. Verify EXCEPTIONS array in pr-router-parity.yml includes "Statusline"
3. `git add tooling/claude/hooks/statusline.sh tooling/claude/CLAUDE.md .github/workflows/pr-router-parity.yml CHANGELOG.md`
4. `git commit -m "feat(claude): add opt-in statusline hook showing context% and cache age (#156)"`
5. `git push origin agent/agy/claude-sonnet-4-6/feat/statusline-156`
6. `gh pr create --title "feat(claude): add opt-in statusline hook showing context% and cache age (#156)" --body "Closes #156

Adds tooling/claude/hooks/statusline.sh — a PostToolUse async hook (opt-in) that prints a one-line session cost summary after each tool call. Requires token-log.sh (PR #386) to be enabled first. Context% is estimated from cumulative session tokens / AAK_CONTEXT_WINDOW (default 200k)." --base master`

---

### Acceptance criteria

- [ ] `tooling/claude/hooks/statusline.sh` exists and is executable
- [ ] shellcheck passes
- [ ] CLAUDE.md has `## Statusline (opt-in)` section documenting wiring
- [ ] pr-router-parity EXCEPTIONS includes "Statusline"
- [ ] CHANGELOG entry added
- [ ] Output is one line: `[aak] ctx: N% (~X tok) | N calls | cache: <age>`
