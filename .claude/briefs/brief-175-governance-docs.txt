## Issue #175 — README + CHANGELOG hygiene for installable governance assets

**Task type:** docs
**Branch to create:** agent/agy/claude-sonnet-4-6/docs/governance-docs-175

---

### Start here — ALWAYS do this first before any file changes

```bash
git checkout master
git pull origin master
git checkout -b agent/agy/claude-sonnet-4-6/docs/governance-docs-175
```

---

### Background

PR #378 (issue #172) merged and made 5 governance templates installable into
`docs/ai/` of target projects via `scripts/install.sh --profile full`.
The files are:
- `project-template/CONTEXT_GOVERNANCE.md`
- `project-template/SUBAGENT_GOVERNANCE.md`
- `project-template/MCP_POLICY.md`
- `project-template/MODEL_ROUTING.md`
- `project-template/CONTEXT_SANITIZATION.md`

The README already mentions them on one line (line 114). This issue asks for:
1. A dedicated README section with more detail on each template and what to fill in
2. A link added to CONTRIBUTING.md
3. A CHANGELOG migration note

---

### File 1 — Update `README.md`

Read README.md first. Find the block around line 114:
```
Then fill in `docs/ai/PROJECT.md` and `docs/ai/COMMANDS.md` in your project.
Governance policy templates (CONTEXT_GOVERNANCE, SUBAGENT_GOVERNANCE, MCP_POLICY,
MODEL_ROUTING, CONTEXT_SANITIZATION) are now also installed as editable
project-owned templates.
```

Replace that sentence with a richer paragraph:

```markdown
Then fill in `docs/ai/PROJECT.md` and `docs/ai/COMMANDS.md` in your project.

#### Governance templates

A full install (`--profile full`, the default) also copies five governance policy
templates into your project's `docs/ai/` directory:

| File | Purpose | Fill in |
|---|---|---|
| `CONTEXT_GOVERNANCE.md` | Context thresholds, compaction policy, router budgets | Team-specific thresholds, project CI commands |
| `SUBAGENT_GOVERNANCE.md` | When to delegate vs. work inline; no-duplicate-reading rules | Project-specific subagent registry |
| `MCP_POLICY.md` | Which MCP servers are allowed, scopes, approval process | Your approved server list |
| `MODEL_ROUTING.md` | Model selection rules per task complexity | Your preferred models |
| `CONTEXT_SANITIZATION.md` | What must never appear in briefs or logs | Project-specific secrets and PII patterns |

Each file is **project-owned** — the kit never overwrites them after the first
install. Edit them to match your team's standards. The templates contain
`<!-- TODO: fill in -->` markers to guide customization.
```

---

### File 2 — Update `CONTRIBUTING.md`

Read CONTRIBUTING.md. Find the line that references `docs/ai/WORKFLOW.md` (around
line 13). After that paragraph (or at the end of the "Project docs" or "Getting
started" section), add:

```markdown
### Governance policy templates

Five governance templates live in `docs/ai/` (installed from `project-template/`):
`CONTEXT_GOVERNANCE.md`, `SUBAGENT_GOVERNANCE.md`, `MCP_POLICY.md`,
`MODEL_ROUTING.md`, and `CONTEXT_SANITIZATION.md`. If you are contributing a
change that affects cross-cutting policy (context limits, subagent rules, model
routing, MCP server approvals), update the relevant template in `project-template/`
and document why. The kit ships the templates; projects own their copies.
```

Find a natural place near other docs/ai references — don't just append at the end
if there's a better location.

---

### File 3 — Update `CHANGELOG.md`

Under `## [Unreleased]` → `### Changed` (create the subsection if it doesn't exist),
prepend:

```
- **`docs` — expand README governance section and add CONTRIBUTING.md link (#175).**
  The install-script section now includes a table describing each of the five
  governance templates (`CONTEXT_GOVERNANCE`, `SUBAGENT_GOVERNANCE`, `MCP_POLICY`,
  `MODEL_ROUTING`, `CONTEXT_SANITIZATION`) that a full install copies into
  `docs/ai/`. `CONTRIBUTING.md` now links to these templates for contributors
  making policy-relevant changes.
```

---

### Constraints

- Only touch `README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`
- Keep the existing README structure — insert the governance table inside the
  existing "Option A" subsection, right after the current one-liner on line 114
- Do not change any script files
- Do not remove any existing text — only extend

---

### After making changes

1. `git add README.md CONTRIBUTING.md CHANGELOG.md`
2. `git commit -m "docs: expand governance templates section in README and CONTRIBUTING.md (#175)"`
3. `git push origin agent/agy/claude-sonnet-4-6/docs/governance-docs-175`
4. `gh pr create --title "docs: expand governance templates section in README and CONTRIBUTING.md (#175)" --body "Closes #175

Adds a dedicated Governance templates table to README.md install section, and a contributing note in CONTRIBUTING.md. Both reference the 5 installable templates (CONTEXT_GOVERNANCE, SUBAGENT_GOVERNANCE, MCP_POLICY, MODEL_ROUTING, CONTEXT_SANITIZATION) installed by PR #378." --base master`

---

### Acceptance criteria

- [ ] README has a table listing the 5 governance templates with purpose + fill-in columns
- [ ] CONTRIBUTING.md has a paragraph pointing to the templates
- [ ] CHANGELOG entry under Changed
- [ ] No existing text removed
