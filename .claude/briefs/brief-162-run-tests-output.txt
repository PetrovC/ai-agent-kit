## Issue #162 — Minimal output style for /run-tests

**Task type:** docs (update one command file)
**Branch to create:** agent/agy/claude-sonnet-4-6/docs/run-tests-style-162

---

### Start here — ALWAYS do this first before any file changes

```bash
git checkout master
git pull origin master
git checkout -b agent/agy/claude-sonnet-4-6/docs/run-tests-style-162
```

---

### Background

The `/run-tests` command currently has no output format constraint.
The goal is to add a strict "Minimal output" section that constrains
the final report to a compact template — no narration, fits one screen.

---

### File to change

#### `tooling/claude/commands/run-tests.md`

Read the current file. Then replace the `Report:` section with a structured
output template. The final file should look like this:

```markdown
---
description: Run the test suite for the current change and report results — without fixing failures.
---

Run the test suite for the current change.

Read `docs/ai/COMMANDS.md` first for the exact commands used in this project.

Steps:
1. Identify which modules or layers are touched by the current change.
2. Run the relevant filtered tests first (by module or category).
3. If they pass, run the full suite.
4. Use the `test-runner` subagent if the output is large.

## Output format (strict — no prose)

Use exactly this Markdown table. Do not add narration outside the table and the failures list.

| | Count |
|---|---|
| ✅ Passed | `N` |
| ❌ Failed | `N` |
| ⏭️ Skipped | `N` |
| 🕒 Duration | `Ns` |

**Commands run:** `<command 1>`, `<command 2>`

**Failing tests (top 5):**

| Test | Reason |
|---|---|
| `<TestName>` | `<one-line reason>` |

**Pre-existing failures:** yes / no / unknown

**Not covered:** `<what was not tested and why, or "none">`

Do not fix test failures. Report them only.
```

---

### CHANGELOG.md

Under `## [Unreleased]` → `### Added`, prepend:

```
- **`feat(claude)` — add strict minimal output template to `/run-tests` (#162).**
  `tooling/claude/commands/run-tests.md` now specifies an exact Markdown table
  format for test results: pass/fail/skip counts, commands run, top-5 failing
  tests with one-line reasons, and pre-existing failure flag. No prose allowed
  outside the template.
```

---

### Constraints

- Only change `tooling/claude/commands/run-tests.md` and `CHANGELOG.md`.
- Keep the frontmatter (`---` block) unchanged.
- Keep the Steps section unchanged.
- Only add/replace the Report/Output section.

---

### After making changes

1. `git add tooling/claude/commands/run-tests.md CHANGELOG.md`
2. `git commit -m "feat(claude): add strict minimal output template to /run-tests (#162)"`
3. `git push origin agent/agy/claude-sonnet-4-6/docs/run-tests-style-162`
4. `gh pr create --title "feat(claude): add strict minimal output template to /run-tests (#162)" --body "Closes #162\n\nAdds a strict Markdown table output template to run-tests.md. Constrains the final report to: pass/fail/skip counts, commands run, top-5 failing tests with one-line reasons. No narration allowed." --base master`

---

### Acceptance criteria

- [ ] `tooling/claude/commands/run-tests.md` has the structured output table
- [ ] Output fits one screen (≤ ~30 lines)
- [ ] No prose constraints added to Steps section
- [ ] CHANGELOG entry added
