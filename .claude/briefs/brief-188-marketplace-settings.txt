## Issue #188 — Document strictKnownMarketplaces + enabledPlugins for target projects

**Task type:** docs (README + CONTRIBUTING update — no new code, no new CI)
**Branch to create:** agent/agy/claude-sonnet-4-6/docs/marketplace-settings-188

---

### Start here — ALWAYS do this first before any file changes

```bash
git checkout master
git pull origin master
git checkout -b agent/agy/claude-sonnet-4-6/docs/marketplace-settings-188
```

---

### Background

The kit ships as a Claude plugin. Target projects can:
1. Lock which marketplace sources Claude Code is allowed to install from using
   `strictKnownMarketplaces: true` in their `settings.json`
2. Pre-enable the kit plugin so any team member automatically has it available,
   using `enabledPlugins: ["PetrovC/ai-agent-kit@ai-agent-kit"]`

Neither pattern is documented anywhere in the kit today. The Claude Code
settings documentation defines both keys.

---

### Files to change

#### 1. README.md

Find the `### Option B — Claude plugin marketplace (opt-in, skills only)` section.
After the existing Option B block (before `### Optional artifacts`), add a new
sub-section:

```markdown
#### Pinning and pre-enabling the plugin (team setup)

For teams where everyone should have the plugin active and no unapproved
plugins should be installable, add these two keys to the project
`settings.json` (or `CLAUDE.local.json` for personal overrides):

```json
{
  "strictKnownMarketplaces": true,
  "enabledPlugins": ["PetrovC/ai-agent-kit@ai-agent-kit"]
}
```

- **`strictKnownMarketplaces: true`** — Claude Code will only allow installing
  plugins from sources already listed in your `settings.json` marketplace
  entries. Prevents accidental installs from unknown sources.
- **`enabledPlugins`** — list of plugin IDs that are automatically active for
  every session. The format is `<org>/<repo>@<plugin-name>`, which matches the
  values you pass to `/plugin install`.

> **Note:** `enabledPlugins` does not replace `/plugin marketplace add` on each
> machine; it is a session-activation shortcut — the marketplace source must
> still be present (or be resolvable from the repo clone). Use it alongside a
> shared `settings.json` committed to the project.
```

#### 2. CONTRIBUTING.md

Read the current file first. Find a suitable location (near "install" or "plugin"
topic if one exists; otherwise append at the end before any footer). Add:

```markdown
## Plugin pinning

If your project uses the kit as a Claude plugin, consider committing
`strictKnownMarketplaces: true` and `enabledPlugins: ["PetrovC/ai-agent-kit@ai-agent-kit"]`
to the project `settings.json`. This ensures all contributors automatically have
the plugin active and cannot accidentally install plugins from other sources.
See the README "Option B — Plugin pinning" section for the full snippet.
```

#### 3. CHANGELOG.md

Under `## [Unreleased]` → `### Added`, prepend:

```
- **`docs(claude)` — document `strictKnownMarketplaces` + `enabledPlugins` for target projects (#188).**
  Target projects can now pin the plugin marketplace source and pre-enable the kit plugin
  via two `settings.json` keys. README Option B section and CONTRIBUTING both updated with
  a ready-to-paste snippet.
```

---

### Constraints

- Docs change only. Do not modify any `.github/workflows/`, `tooling/`, `scripts/`,
  or `skills/` files.
- Do not add any `project-template/settings.json` — the project-template directory
  contains only Markdown files and should stay that way.
- Keep the prose factual and concise. No marketing language.

---

### After making changes

1. `git add README.md CONTRIBUTING.md CHANGELOG.md`
2. `git commit -m "docs(claude): document strictKnownMarketplaces + enabledPlugins (#188)"`
3. `git push origin agent/agy/claude-sonnet-4-6/docs/marketplace-settings-188`
4. `gh pr create --title "docs(claude): document strictKnownMarketplaces + enabledPlugins (#188)" --body "Closes #188\n\nDocuments how target projects can pin their Claude plugin marketplace source and pre-enable the kit plugin using \`strictKnownMarketplaces\` and \`enabledPlugins\` in \`settings.json\`." --base master`

---

### Acceptance criteria

- [ ] README has the pinning sub-section with both keys and the JSON snippet
- [ ] CONTRIBUTING.md has a brief "Plugin pinning" section
- [ ] CHANGELOG entry added under [Unreleased] > Added
- [ ] No changes to code or CI files
