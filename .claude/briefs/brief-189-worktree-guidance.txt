## Issue #189 — Worktree settings guidance in CLAUDE.md and CONTEXT_GOVERNANCE.md

**Task type:** docs
**Branch to create:** agent/agy/claude-sonnet-4-6/docs/worktree-guidance-189

---

### Start here — ALWAYS do this first before any file changes

```bash
git checkout master
git pull origin master
git checkout -b agent/agy/claude-sonnet-4-6/docs/worktree-guidance-189
```

---

### Background

Claude Code exposes four worktree-related settings in `.claude/settings.json`:

- `worktree.baseRef` — which git ref new worktrees branch from
  (`"head"` = current branch, `"main"` = main branch)
- `worktree.symlinkDirectories` — directories to symlink instead of copy into
  the worktree (avoids duplicating large `node_modules/` or `vendor/` trees)
- `worktree.sparsePaths` — paths to include when using sparse checkout in
  worktrees (empty array = all files)
- `worktree.bgIsolation` — whether background subagents run in an isolated
  worktree sandbox (`"sandbox"`) or share the current worktree (`"shared"`)

Reference: https://code.claude.com/docs/en/settings

The kit uses worktrees in production (visible under `.claude/worktrees/`). Users
need recommended values and a rationale.

---

### File 1 — Add a new section to `tooling/claude/CLAUDE.md`

Read `tooling/claude/CLAUDE.md` in full first. Find the "Session hygiene" section.
Add a new `## Worktree settings` section **immediately before** or **immediately after**
"Session hygiene" (your choice — pick whichever flows better). Use this content:

```markdown
## Worktree settings

Claude Code can spin up isolated worktrees for background and parallel tasks.
Configure these in `.claude/settings.json`:

```json
{
  "worktree": {
    "baseRef": "head",
    "symlinkDirectories": ["node_modules", "vendor", ".venv"],
    "sparsePaths": [],
    "bgIsolation": "sandbox"
  }
}
```

| Setting | Recommended | Rationale |
|---|---|---|
| `baseRef` | `"head"` | Branch from the current working branch, not `main`. Keeps worktree in sync with in-progress agent work. Use `"main"` only if you always work from a clean main. |
| `symlinkDirectories` | `["node_modules", "vendor", ".venv"]` | Avoids duplicating large dependency trees into each worktree. Add your own heavy directories. |
| `sparsePaths` | `[]` | Empty means all files — suitable for most projects. Use sparse paths only if the repo is unusually large (monorepo with >10 GB). |
| `bgIsolation` | `"sandbox"` | Background subagents run in their own worktree sandbox. Prevents parallel agents from overwriting each other's in-progress edits. |

These settings are not installed by the kit's `install.sh` script — they are
intentionally left to the user to configure, since values depend on the target
project's tech stack. The `settings.json` template installed by the script leaves
the `worktree` block commented out as a starting point.
```

---

### File 2 — Add a short reference to `docs/ai/CONTEXT_GOVERNANCE.md`

Read `docs/ai/CONTEXT_GOVERNANCE.md` in full. Near the end of the file (or after
the "Provider settings that enforce these thresholds" section), add:

```markdown
## Worktree isolation

When Claude Code runs background agents it can place them in isolated git
worktrees. Set `worktree.bgIsolation: "sandbox"` in `.claude/settings.json` to
ensure parallel agents cannot overwrite each other's working files.

See the **Worktree settings** section of `tooling/claude/CLAUDE.md` for the full
recommended configuration and rationale.
```

---

### File 3 — Update `CHANGELOG.md`

Under `## [Unreleased]` → `### Added`, prepend:

```
- **`docs(claude)` — add worktree settings guidance to CLAUDE.md and CONTEXT_GOVERNANCE.md (#189).**
  Documents recommended values for `worktree.baseRef`, `worktree.symlinkDirectories`,
  `worktree.sparsePaths`, and `worktree.bgIsolation` in `tooling/claude/CLAUDE.md`
  with rationale table. Adds a short cross-reference in `docs/ai/CONTEXT_GOVERNANCE.md`.
```

---

### Constraints

- Only touch `tooling/claude/CLAUDE.md`, `docs/ai/CONTEXT_GOVERNANCE.md`, `CHANGELOG.md`
- Do not change any script, hook, or settings.json file
- Keep additions concise — one section per file, no repetition

---

### After making changes

1. `git add tooling/claude/CLAUDE.md docs/ai/CONTEXT_GOVERNANCE.md CHANGELOG.md`
2. `git commit -m "docs(claude): add worktree settings guidance (#189)"`
3. `git push origin agent/agy/claude-sonnet-4-6/docs/worktree-guidance-189`
4. `gh pr create --title "docs(claude): add worktree settings guidance (#189)" --body "Closes #189

Adds a Worktree settings section to tooling/claude/CLAUDE.md documenting recommended values for baseRef, symlinkDirectories, sparsePaths, bgIsolation with rationale. Cross-reference added to docs/ai/CONTEXT_GOVERNANCE.md." --base master`

---

### Acceptance criteria

- [ ] CLAUDE.md has a Worktree settings section with a table
- [ ] CONTEXT_GOVERNANCE.md has a short cross-reference
- [ ] CHANGELOG entry added
